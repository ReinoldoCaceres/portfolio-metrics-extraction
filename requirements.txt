# Portfolio Metrics Extraction — all deps for: python run.py, pytest, review.ipynb
# Requires Python 3.10+

# Pipeline (default replay mode uses cache only; openai needed for --extract)
openai>=1.40.0
pdfplumber>=0.11.0
pandas>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Tests (README: python -m pytest -q)
pytest>=8.0

# Review notebook (README: jupyter lab review.ipynb)
jupyterlab>=4.0.0
ipykernel>=6.0
